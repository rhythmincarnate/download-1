
1.表达式的转换
	中缀表达式---->前缀(波兰序)
		<1>初始化两个栈,一个用于存放转换结果，一个用于存放运算符
		<2>将中缀表达式至右向左遍历,如果是操作数,直接入结果栈
		<3>如果是运算符与运算符栈栈顶元素比较优先级,决定是否入运算符栈
			@1 栈为空　　直接入
			@2 栈顶元素运算符优先级高于待入栈运算符,将此栈顶运算符入结果栈	
				待入栈运算符再与栈顶元素比较
			@3 栈顶元素运算符等于待入栈运算符,直接入运算符栈
			@4 栈顶元素是右括号，则直接入栈
			@5 栈顶元素优先级低于待入栈运算符，则其直接入运算符栈
		<4>如果遍历的字符是右括号直接入运算符栈,如果是左括号,
			将运算符栈中的栈顶运算符出栈判断是否是与其匹配的右括号，
			如果是则继续遍历，如果不是则将出栈运算符入结果栈,再次判断

	中缀表达式---->后缀(逆波兰序)
		<1>初始化一个栈用于存放运算符,一个队列用于存放转换结果
		<2>将中缀表达式至左向右遍历,如果是操作数,直接入结果队
		<3>如果是运算符与运算符栈栈顶元素比较优先级,决定是否入运算符栈
			@1 栈为空　　直接入
			@2 栈顶元素运算符优先级大于等于待入栈运算符,将此栈顶运算符入结果队	
				待入栈运算符再与栈顶元素比较
			@3 栈顶元素是左括号，则直接入栈
			@4 栈顶元素优先级低于待入栈运算符，则其直接入运算符栈
		<4>如果遍历的字符是左括号直接入运算符栈,如果是右括号,
			将运算符栈中的栈顶运算符出栈判断是否是与其匹配的左括号，
			如果是则继续遍历，如果不是则将出栈运算符入结果队,再次判断

"1+2*3"
"(1+2)*3"
"+1*23"

2.球钟问题





